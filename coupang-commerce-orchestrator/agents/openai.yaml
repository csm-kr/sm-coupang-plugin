interface:
  display_name: "쿠팡 커머스 오케스트레이터"
  short_description: "현재 단계·차단·다음 승인 질문을 한 화면에 안내"
  default_prompt: "Use $coupang-commerce-orchestrator to inspect the current commerce project, show the active stage, and ask only the next required user question."
