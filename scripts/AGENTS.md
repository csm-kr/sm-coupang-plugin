# 개발 스크립트 지침

이 디렉터리의 코드는 저장소 전 단계가 의존하는 결정적 도구다.

- 변경 전 `../tests/harness/`에 실패 테스트를 먼저 추가한다.
- CLI는 기계 판독 가능한 종료 코드와 구체적인 실패 메시지를 반환한다.
- 경로 처리는 Windows와 POSIX 구분자를 모두 허용하고 저장소 밖으로 벗어나지 않게 검증한다.
- 정책 계산과 훅 I/O를 분리해 순수 함수 단위 테스트가 가능하게 유지한다.
- 완료 전 `python tdd.py verify harness`를 저장소 루트에서 실행한다.
